{
  "extends": "@vue/tsconfig/tsconfig.web.json",
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue", "src/typings/**/*", "build、*"],
  "exclude": ["node_modules", "dist", "**/*.js", "vite.config.ts"],
  "compilerOptions": {
    "baseUrl": ".",
    "types": [
        "node"
      ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },

  "references": [
    {
      "path": "./tsconfig.node.json"
    }
  ]
}
